import XCTest
@testable import SwiftHOV

final class SwiftHOVTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftHOV().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
